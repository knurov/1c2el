database:
  # dryRun: true
  host: localhost
  port: 5432
  db: electrolab
  user: electrolab
  passwd: electrolab
files:
  src: ./examples
  # done: ./examples/done
  error:
  nameTemplate: .*\.xml
log:
  level: Trace
  file:
# https://yourbasic.org/golang/regexp-cheat-sheet/
rules:
  - name: "Общее правило"
    regexp: "(?P<short>.+?)[_\\s](?P<prop>.+?)[-/]"
    transformer:
      - field: fullName
        position: 1
        # regexp: .*
        # value: 10
      - field: type
        position: 2
    coil: []